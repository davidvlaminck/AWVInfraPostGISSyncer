[project]
name = "awvinfra-postgis-syncer"
version = "1.0.0"
description = "AWV Infra PostGIS Syncer"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32.4",
    "psycopg2-binary>=2.9.10",
    "pyjwt>=2.10.1",
    "colorama>=0.4.6",
    "cryptography>=45.0.6",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
